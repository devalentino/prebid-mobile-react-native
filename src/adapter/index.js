import Adapter from './Adapter';
import PrebidServerAdapter from './PrebidServerAdapter';

export const types = {
  PREBID_SERVER_ADAPTER: 'PREBID_SERVER_ADAPTER',
}

export default Adapter;
export { PrebidServerAdapter };
